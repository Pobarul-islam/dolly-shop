import React from 'react';
import Layout from '../../Components/Layout';

const Login = () => {
    return (
        <Layout>
            <h2>Login Page</h2>
        </Layout>
    );
};

export default Login;