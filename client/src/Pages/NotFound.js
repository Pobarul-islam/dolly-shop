import React from 'react';
import Layout from '../Components/Layout';

const NotFound = () => {
    return (
        <Layout>
            <h2>NotFoutd 404</h2>
        </Layout>
    );
};

export default NotFound;